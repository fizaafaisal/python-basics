#Arguments

#Information can be passed into function as arguments.
#Arguments are specified after the function name,
# inside  the parantheses.
# ypu can add as many arguments as you want,
# just seperate them with a comma.

def myfunc(fname):
    print(fname + "fiza")

myfunc("Emil")
myfunc("Tobias")
myfunc("linus")
