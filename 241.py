#Write a Python program that defines a function named greet_user.
# This function should take a single parameter, name,
# and print the message: "Hello, [name]!"
# (where [name] is replaced with the actual value of the parameter).
# After defining the function, call it with the name "Alice".

def greet_user(name):
    print(f"hello, {name}!")
greet_user("fiza")
